{
  "name": "mantleflow-credit-contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy:mantle": "hardhat run scripts/deploy.js --network mantle",
    "deploy:validation": "hardhat run scripts/deploy_validation.js --network mantle",
    "register:agent": "hardhat run scripts/register_blockscam_agent.js --network mantle"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "5.0.2",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.10"
  }
}
